# ──────────────────────────────────────────────────────────────
# VIBE CHECK — Environment Variables (copy to .env and fill in)
# ──────────────────────────────────────────────────────────────

# ── PostgreSQL ────────────────────────────────────────────────
POSTGRES_USER=vibecheck
POSTGRES_PASSWORD=vibecheck_secret
POSTGRES_DB=vibecheck
POSTGRES_PORT=5432

# ── Service Ports ─────────────────────────────────────────────
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ── PRIMARY Sponsor APIs ─────────────────────────────────────
# Yutori — primary reasoning engine (platform.yutori.com/sign-up)
YUTORI_API_KEY=yt-your-key-here
# Neo4j — knowledge graph (console.neo4j.io)
# Local (Docker): bolt://neo4j:7687  |  Cloud: neo4j+s://xxxx.databases.neo4j.io:7687
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=vibecheck_neo4j
# Tavily — supplemental web search (app.tavily.com)
TAVILY_API_KEY=tvly-your-key

# ── BACKUP / SUPPLEMENTAL ────────────────────────────────────
# OpenAI — fallback reasoning (platform.openai.com)
OPENAI_API_KEY=sk-your-key
# Fastino — fast entity extraction (fastino.ai)
FASTINO_API_KEY=your-fastino-key

# ── GitHub (optional — raises rate limits) ────────────────────
GITHUB_TOKEN=ghp_your-token

# ── App Config ────────────────────────────────────────────────
DASHBOARD_WEBHOOK_URL=http://localhost:3000/api/events
DASHBOARD_WEBHOOK_SECRET=change-me-to-a-random-32-char-secret
